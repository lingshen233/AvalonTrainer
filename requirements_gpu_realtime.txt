# 实时GPU TFLOP检测器依赖包

# 必选依赖 (GPU检测和基本功能)
torch>=1.9.0                # PyTorch - GPU检测和性能测试
torchvision>=0.10.0         # torchvision - 配套包

# 可选依赖 (增强功能)
nvidia-ml-py3>=7.352.0      # pynvml - NVIDIA GPU监控
GPUtil>=1.4.0               # GPUtil - 替代GPU监控

# 系统工具依赖
psutil>=5.8.0               # 系统资源监控

# 注意事项:
# 1. torch安装需要根据CUDA版本选择对应版本
#    CPU版本: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#    CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 2. nvidia-ml-py3需要系统已安装NVIDIA驱动
#
# 3. 基本功能只需要nvidia-smi命令即可工作 